// pages/order-history.tsx
import { useEffect } from 'react';

const OrderHistoryPage = () => {
    useEffect(() => {
        // Fetch order history data or perform any initialization
    }, []);

    return (
        <div>
            <h1>Order History</h1>
            {/* Add your order history content here */}
        </div>
    );
};

export default OrderHistoryPage;
